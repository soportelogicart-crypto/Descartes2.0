# Desarrollo: dejar vacio para que Vite envie /api al backend (vite.config.ts).
# VITE_API_BASE_URL=

# Solo si el frontend llama a la API directamente (requiere CORS_ORIGINS en la API):
# VITE_API_BASE_URL=http://localhost/descartes-api/public

# Servidor PHP integrado (sin XAMPP):
# VITE_API_BASE_URL=http://127.0.0.1:8080
